/*
    ### 5. Combining Arrays

    **Instructions:**

    1. Create two arrays of your choice.
    2. Use the concat method to combine the two arrays into a new array.
    3. Print both the original arrays and the combined array using console.log().
*/

const name3 = ['Rohim', 'korim', 'jobbar'];

const name2 = ['hakim', 'abbas'];

const numsum = name2.concat(name3)

// const numsum = name2 + name3;

console.log(numsum);

console.log(numsum.length);
